
services:
    nginx-archeshedatatransformation:
      container_name: nginx-archeshedatatransformation
      image: nginx:latest
      ports:
        - "8080:80"
      volumes:
        - ./docker/nginx-config/default.conf:/etc/nginx/conf.d/default.conf
      networks:
        - archeshedatatransformation-network

    db-archeshedatatransformation:
      container_name: db-archeshedatatransformation
      image: postgis/postgis:14-3.2
      volumes:
        - postgres-data:/var/lib/postgresql/data
        - postgres-log:/var/log/postgresql
        - ./docker/init-unix.sql:/docker-entrypoint-initdb.d/init.sql # to set up the DB template
      ports:
        - '5433:5432'
      env_file: 
        - ./docker/env_file.env
      networks:
        - archeshedatatransformation-network

    elasticsearch-archeshedatatransformation:
      container_name: elasticsearch-archeshedatatransformation
      image: docker.elastic.co/elasticsearch/elasticsearch:8.10.4
      volumes:
        #- ../../elasticsearch_data_arches_he_data_transformation:/usr/share/elasticsearch/data
        - elasticsearch-data:/usr/share/elasticsearch/data
      ports:
        - "9201:9200"
        - "9301:9300"
      env_file: 
        - ./docker/env_file.env
      networks:
        - archeshedatatransformation-network

    rabbitmq-archeshedatatransformation:
      container_name: rabbitmq-archeshedatatransformation
      image: rabbitmq:3.12.6-management
      hostname: my-rabbit
      volumes:
        - rabbitmq-data:/var/lib/rabbitmq/mnesia/rabbit@my-rabbit
        - rabbitmq-logs:/var/log/rabbitmq/log
      ports:
        - 5673:5672
        - 15673:15672
      networks:
        - archeshedatatransformation-network

    #pg-feat-archeshedatatransformation:
    #  container_name: pg-feat-archeshedatatransformation
    #  image: pramsey/pg_featureserv
    #  volumes:
    #    - ../../pg_featureserv_config_arches_he_data_transformation:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-archeshedatatransformation:5432/arches_he_data_transformation
    #  ports:
    #    - 9000:9000
    #  depends_on:
    #    - db-archeshedatatransformation
    #  networks:
    #    - archeshedatatransformation-network
    
    #pg-tile-archeshedatatransformation:
    #  container_name: pg-tile-archeshedatatransformation
    #  image: pramsey/pg_tileserv
    #  volumes:
    #    - ../../pg_tileserv_config_arches_he_data_transformation:/config
    #  environment:
    #    - DATABASE_URL=postgresql://arches_spatial_views:arches_spatial_views@db-archeshedatatransformation:5432/arches_he_data_transformation
    #  ports:
    #    - 7800:7800
    #  depends_on:
    #    - db-archeshedatatransformation
    #  networks:
    #    - archeshedatatransformation-network


networks:
  archeshedatatransformation-network:
    name: archeshedatatransformation-network
    driver: bridge

volumes:
    postgres-data:
    postgres-log:
    elasticsearch-data:
    rabbitmq-logs:
    rabbitmq-data:
